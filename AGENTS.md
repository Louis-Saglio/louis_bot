# louis_bot agent notes

## Code comments

Every variable declaration must carry a comment stating its type, e.g.
`// dict: worker id -> assignment record`. This is the one exception to the
global "comments explain why, never what" rule.
